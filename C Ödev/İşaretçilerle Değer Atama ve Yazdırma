#include <stdio.h>

int main() {
    int num1 = 42;
    int num2 = 73;

    int *ptr1 = &num1;
    int *ptr2 = &num2;

    printf("Değer 1: %d\n", *ptr1);
    printf("Değer 2: %d\n", *ptr2);

    return 0;
}
