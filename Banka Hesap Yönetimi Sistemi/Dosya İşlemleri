void saveUsersToFile(struct User *users, int userCount) {
    // Kullanıcı bilgilerini bir dosyaya kaydet
}

void loadUsersFromFile(struct User *users, int *userCount) {
    // Dosyadan kullanıcı bilgilerini yükle
}
