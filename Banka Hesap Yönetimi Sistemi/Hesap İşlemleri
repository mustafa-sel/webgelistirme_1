// Kullanıcı giriş işlemi
struct User *login(struct User *users, int userCount, int accountNumber, float password) {
    // Hesap numarası ve şifre ile giriş yap ve ilgili kullanıcıyı döndür
}

// Hesap bilgilerini görüntüleme
void viewAccount(struct User *user) {
    printf("Ad: %s\nHesap Numarası: %d\nBakiye: %.2f\n", user->name, user->accountNumber, user->balance);
}

// Para yatırma işlemi
void deposit(struct User *user, float amount) {
    // Belirli miktarda parayı hesaba yatır
}

// Para çekme işlemi
void withdraw(struct User *user, float amount) {
    // Belirli miktarda parayı hesaptan çek
}
